plugins {
    id 'java'
}

group 'org.dru.dusap'
version '1.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.dru.dusap:dusap-messaging:1.0'
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
